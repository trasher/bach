{% extends 'AdministrationBundle:Default:main.html.twig' %}

{% block container %}
        <form class="form-inline" action="{{ path('administration_copyfields') }}" method="POST" {{ form_enctype(form) }}>
            <fieldset>
                <legend>{{ _('Copy fields') }}</legend>
                <div class="btn-toolbar">
                    <button name="selectAll" type="submit" class="jbtn" >{{ _('Select all') }}</button>
                    <button name="deselectAll" type="submit" class="jbtn">{{ _('Unselect all') }}</button>
                    <button name="add" type="submit" class="jbtn" >{{ _('Add') }}</button>
                    <button name="remove" type="submit" class="btn btn-delete">{{ _('Delete') }}</button>
                </div>


                {# iterate over each existing tag and render its only field: name #}
                {% for field in form.copyFields %}
                    <div class="well">
                        <div class="row-fluid show-grid">
                            <div class="span4">{{ _('Source:') }} {{ form_widget(field.source) }}</div>
                            <div class="span4">{{ _('Destination:') }} {{ form_widget(field.dest) }}</div>
                            <div class="span4">{{ _('Max Chars:') }} {{ form_widget(field.maxChars) }}</div>
                        </div>
                    </div>
                    {% endfor %}
                    {{ form_widget(form._token) }}
                    <button name="submit" type="submit" class="jbtn">{{ _('Validate') }}</button>
            </fieldset>
        </form>
    </div>
{% endblock %}
